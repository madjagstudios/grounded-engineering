# GE-20 Work Notes

- Goal recorded before implementation: add an offline, maintainer-only helper that emits current `validated_against` entries by card ID or path without mutating cards or validation status.
- 2026-08-29: Started from `main` after fast-forwarding; canonical repository root was verified.
- 2026-08-29: Shared code-map check is pre-existingly red because `INDEX.md`, `GUIDE.md`, and `misc.md` are missing; kept outside GE-20 scope.
- 2026-08-29: Release metadata remains intentionally unchanged while additional tickets are completed before the next version revision.
- 2026-08-29: The command emits source-order `validated_against` YAML for a card ID or repository-relative path; `--check` returns 1 for missing or stale provenance and 0 for a current match.
- 2026-08-29: Initial verification on `28577f6` passed `npm test` with 192/192 tests, emitted `CODEX-SAFETY-POLICY` pinned to `03861e69ef549717c0fc7045abad56321d4a082b`, and returned exit 1 for the expected missing-provenance `--check` result.
- 2026-08-29: A copied checkout whose directory name contained spaces produced the same scaffold and its card checksum was unchanged; no network or repository write was used.
- 2026-08-29: Independent exact-head review found and the follow-up fixed three issues: matching provenance is not current unless status is `validated`, unrelated card errors do not block selected-card scaffolding, and CLI tests resolve paths with `fileURLToPath`; final `npm test` passed 195/195 on `ce74592` with no Critical or Important findings remaining.
